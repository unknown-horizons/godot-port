# Tools used during development

## python
Create conda environment:
```shell
cd DevTools
conda create --channel conda-forge --prefix ./conda python numpy pillow matplotlib pandas jupyter notebook
conda activate ./conda
```
